# 面向低照度复杂场景的文本检测与识别优化研究 —— 工作进展汇报

本文基于导师课题方向缩小场景范围后整理，2026.5.25

## 一、课题调整说明

原始方向：

基于深度学习的任意形状场景文本检测与识别方法研究

存在问题：

研究范围较大
包含检测 + 识别 + 任意形状文本
创新点分散
实验周期长

结合导师建议，拟将研究重点聚焦于：

低照度复杂场景（夜间、弱光、反光环境）

调整后的研究目标：

复杂低照度场景
+
文本检测优化
+
文本识别优化

形成更明确的问题驱动：

低光环境
↓
文本边缘模糊
↓
检测困难 / 识别下降
↓
设计优化方法

最终题目：

基于深度学习的面向低照度复杂场景的文本检测与识别优化研究

---

# 二、方案具体设想

整体流程：

```
Input Image
 ↓
Low-light Enhancement
 ↓
Text Detection
 ↓
Text Region Cropping
 ↓
Transformer OCR
 ↓
Recognized Text
```

核心思想：

不是重新设计完整 OCR，

而是在成熟框架基础上：

```text
低照度增强
+
检测优化
+
识别优化
```

---

## 2.1 低照度增强模块

低照度主要问题：

```text
亮度低
噪声高
文字边缘缺失
反光干扰
```

计划采用：

```text
Zero-DCE
Retinex
轻量增强网络
```

增强：

```text
I_enh = F(I_low)
```

目的：

恢复字符纹理。

---

## 2.2 文本检测优化

检测 baseline：

优先：

```text
DBNet
```

原因：

```text
适合任意形状文本
检测性能稳定
开源成熟
```

优化方向：

加入：

```text
SE / CBAM attention
FPN 多尺度融合
```

目标：

提升：

```text
夜间小目标文本
模糊文本
低对比度文本
```

检测能力。

---

## 2.3 文本识别优化

识别重点作为主要创新部分。

Baseline：

```text
ABINet
或
TrOCR
```

计划：

加入：

```text
CNN local feature
+
Transformer global feature
```

融合：

```text
f = fusion(local, global)
```

解决：

```text
字符模糊
粘连
细节缺失
```

---

## 2.4 光照引导 Attention

普通 attention：

容易关注背景。

拟设计：

```text
Illumination-guided Attention
```

即：

利用亮度信息：

```text
alpha = sigmoid(g(I))
```

引导：

```text
text feature attention
```

目的：

增强字符区域，
抑制噪声。

预计作为主要创新点。

---

# 三、Backbone 推进计划

建议顺序：

---

## 3.1 DBNet（检测 baseline）

用途：

```text
建立检测实验
验证低照度影响
```

---

## 3.2 CRNN（识别 baseline）

用途：

```text
快速跑通 OCR
验证增强有效性
```

---

## 3.3 ABINet（主实验）

用途：

```text
完成识别优化
完成主要结果
```

作为：

```text
Main Backbone
```

---

## 3.4 TrOCR（泛化验证）

目标：

证明方法具有迁移能力。

---

# 四、数据集计划

基础：

```text
ICDAR2015
SVT
IIIT5K
```

用于 baseline。

---

低照度数据：

计划：

```text
人工降亮度
加入噪声
模糊增强
```

构造：

```text
Low-light OCR Dataset
```

必要时自建小规模测试集。

---

# 五、实验推进顺序

```text
1. 跑通 DBNet + CRNN
2. 加入低照度增强
3. 完成检测实验
4. 完成识别实验
5. 加入 Attention 创新
6. 消融实验
7. ABINet / TrOCR 泛化
```

---

# 六、当前阶段总结

当前课题已从：

```text
任意形状场景文本检测与识别
```

收缩至：

```text
低照度复杂场景文本检测与识别
```

优势：

```text
问题明确
创新集中
实验可完成
符合本科毕设周期
```

后续重点：

明确主创新点，

预计聚焦：

> 光照引导注意力 + Transformer 文本识别优化
