## 阶段总结：

**课题**: 基于深度学习的面向低照度复杂场景的文本检测与识别优化研究

---

### 一、已完成工作

1. 搭建 MMOCR 实验框架（检测: DBNet / 识别: CRNN）
2. 实现低照度数据退化脚本（HLS 空间亮度衰减 + 高斯噪声）
3. 完成 Phase 1 二元对比（正常 vs 低光照）—— 证实低光照导致性能显著下降
4. 搭建 SigLIP 语义引导热力图原型
5. 实现 SigLIP 引导的低光照推理增强 pipeline（热力图 → 文字区域自适应提亮）
6. 完成 Phase 2 三元对比（正常 vs 低光照 vs SigLIP 增强）—— 整体恢复率 56%

---

### 二、实验流水线（Phase 1-2 已跑通）

`
demo/ (正常光照, 5张)
  │
  ├─→ create_lowlight_dataset.py  ──→  lowlight_dataset/ (低光照退化)
  │
  ├─→ p1.py (Phase 1)
  │     DBNet+CRNN: 正常 vs 低光照
  │     → 结论: 低光照显著降低检测/识别数量
  │
  ├─→ siglip_prototype.py (Phase 2 定性)
  │     SigLIP prompt 热力图: 正常 vs 低光照 vs 差异
  │     → 结论: 低光照使文本区域响应减弱,但未完全失效
  │
  ├─→ siglip_enhance.py (Phase 2 增强)
  │     "清晰文字"热力图 → HLS L通道自适应提亮
  │
  └─→ p2_compare.py (Phase 2 对比)
        DBNet+CRNN: 正常 vs 低光照 vs SigLIP增强
        → 结论: 增强恢复率 56%
`

---

### 三、Phase 2 核心结果（5 张 demo 图）

| 图片 | 正常 | 低光照 | SigLIP增强 | 变化 |
|------|------|--------|------------|------|
| densetext_det | 1724 | 1494 | 1651 | +11% |
| kie | 90 | 62 | 62 | 0% |
| text_det | 23 | 14 | 27 | +93% |
| text_ocr | 19 | 2 | 2 | 0% |
| text_recog | 19 | 0 | 0 | 0% |

- 低光照使检测数平均下降 60.6 个/图
- SigLIP 增强恢复 34.0 个/图, **恢复率 56%**
- 密集文本场景效果突出（+11% ~ +93%）, 稀疏文本场景受限于 patch 粒度过粗

---

### 四、关键发现

1. 低光照导致 DBNet+CRNN 性能显著下降, 部分场景完全失效（text_recog: 19→0）
2. SigLIP 语义特征对光照有鲁棒性: 低光照下响应减弱但未消失
3. 仅推理增强的恢复效果受 patch 粒度限制, 需从训练端深度结合

---

### 五、方向调整: CRNN → SVTR + SigLIP 训练端融合

**问题**: CRNN (2015) 架构过老, ResNet+RNN 与 SigLIP 的 ViT 结构不匹配, 限制了融合深度。

**方案**: 识别 backbone 从 CRNN 切换至 SVTR (2022, ViT 结构), 与 SigLIP 同架构家族, 特征天然对齐。

`
SVTR = 图片切 patch → patch embedding → Transformer encoder → 识别输出
SigLIP = 图片切 patch → patch embedding → Transformer encoder → 图文对比
               ↑                                 ↑
          同一表征空间, 可直接做特征级融合 / 蒸馏 / 对齐
`

**升级后的整体框架**:

`
低光照图片
    │
    ├─→ DBNet (检测)                  ← 保持不变
    │
    └─→ SVTR (识别, ViT backbone)     ← 新
            │
            ├─→ 主分支: 识别 head → 识别 loss (CTC/CE)
            │
            └─→ 辅助分支: SigLIP (frozen) → 语义对齐 loss ← 创新
                    │
            L_total = L_rec + λ · L_distill
`

---

### 六、创新方向菜单（按学长建议）

**核心原则**: 加模块 / 改 loss / 加分支 / 减 patch 速度不降 —— 任选 1-3 个组合。

| 方向 | 具体做法 |
|------|---------|
| **换 backbone** | CRNN → SVTR (ViT), 与 SigLIP 同构 |
| **加语义对齐 loss** | SVTR 特征与 SigLIP 特征做 cosine/contrastive distillation |
| **加 cross-attention 模块** | SVTR 层间插入 cross-attn, query=SVTR, key/value=SigLIP |
| **加双分支融合** | 主分支 SVTR + 辅助分支 SigLIP(frozen) → concat/fusion → head |
| **SigLIP 引导 attention bias** | SigLIP "清晰文字"热力图作为 SVTR attention map 的偏置项 |
| **减 patch 加速** | SVTR 用更大 patch_size (少 token), SigLIP 特征补偿丢失细节 |

**目前准备选择组合**:

> SVTR backbone + SigLIP 语义对齐蒸馏 loss
>
> 逻辑链: ViT 结构使 patch 级特征天然对齐 → SigLIP 提供光照无关的语义监督 → SVTR 在训练中学会对光照鲁棒的文本表征 → 低光照识别率提升
>
> 消融实验: (1) CRNN baseline (2) SVTR baseline (3) SVTR + 亮度增强 (4) SVTR + SigLIP 蒸馏 (5) 完整 pipeline

---

### 七、Phase 3 实施计划

| 步骤 | 内容 | 预计产出 |
|------|------|---------|
| 1 | 确认 SVTR 在 MMOCR 环境中可用 | MMOCRInferencer(rec='SVTR-tiny') 跑通 |
| 2 | 跑 SVTR + CRNN 正常 vs 低光照 baseline | Phase 3 基础对照数据 |
| 3 | 实现 SigLIP 语义蒸馏 loss 模块 | SigLIPDistillationLoss |
| 4 | 接入 SVTR 训练流程 | 训练配置 + loss 权重 |
| 5 | 对比实验: baseline vs +蒸馏 vs +推理增强 | 核心实验表格 |
| 6 | 消融实验: 蒸馏权重 λ / 模型结构对比 | 消融表格 |

---

**### 八、论文结构预期**

`
第一章 绪论
第二章 相关工作 (OCR + 低光照增强 + VLM/知识蒸馏)
第三章 方法
  3.1 整体框架
  3.2 低光照数据退化
  3.3 SVTR 识别 backbone
  3.4 SigLIP 语义对齐蒸馏 ← 核心创新
第四章 实验
  4.1 实验设置
  4.2 低光照对 OCR 的影响 (Phase 1)      ← 问题验证
  4.3 SigLIP 语义引导增强实验 (Phase 2)   ← 初步方案
  4.4 SVTR + SigLIP 蒸馏实验 (Phase 3)   ← 核心贡献
  4.5 消融实验
第五章 总结与展望
`

---

**### 九、文件清单**

| 文件 | 阶段 | 用途 |
|------|------|------|
| create_lowlight_dataset.py | Phase 1 | 正常图 → 低光照退化 |
| p1.py | Phase 1 | 正常 vs 低光照 二元对比 |
| siglip_prototype.py | Phase 2 | SigLIP prompt 热力图定性分析 |
| siglip_enhance.py | Phase 2 | SigLIP 语义增强生成 |
| p2_compare.py | Phase 2 | 正常 vs 低光照 vs 增强 三元对比 |
| p3_svtr_baseline.py | Phase 3 (待写) | SVTR 正常 vs 低光照 baseline |
| siglip_distill_loss.py | Phase 3 (待写) | SigLIP 语义蒸馏 loss 模块 |
| p3_train.py | Phase 3 (待写) | SVTR + SigLIP 蒸馏训练 + 评估 |