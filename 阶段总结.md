## 阶段总结：低照度文本检测与识别基线实验 + SigLIP 语义引导增强

**课题**: 基于深度学习的面向低照度复杂场景的文本检测与识别优化研究

---

### 一、已完成工作

1. 搭建 MMOCR 实验框架（DBNet + CRNN）
2. 实现低照度数据退化脚本（HLS 空间亮度衰减 + 高斯噪声）
3. 完成 Phase 1 二元对比（正常 vs 低光照）
4. 搭建 SigLIP 语义引导热力图原型
5. 实现 SigLIP 引导的低光照增强 pipeline
6. 完成 Phase 2 三元对比（正常 vs 低光照 vs SigLIP 增强）

---

### 二、实验流水线

```
demo/ (正常光照, 5张)
  │
  ├─→ create_lowlight_dataset.py  ──→  lowlight_dataset/ (低光照退化)
  │
  ├─→ p1.py (Phase 1 二元对比)
  │     DBNet+CRNN: 正常 vs 低光照
  │     → outputs/phase1_comparison/
  │     → 结论: 低光照显著降低检测/识别数量
  │
  ├─→ siglip_prototype.py (Phase 2 定性分析)
  │     SigLIP 4 种 prompt 热力图: 正常 vs 低光照 vs 差异
  │     → outputs/siglip_heatmaps/
  │     → 结论: 低光照使文本区域响应减弱,注意力向背景扩散,但未完全失效
  │
  ├─→ siglip_enhance.py (Phase 2 增强生成)
  │     "清晰文字" prompt 热力图 → HLS L通道自适应提亮
  │     → outputs/siglip_enhanced/
  │
  └─→ p2_compare.py (Phase 2 三元对比)
        DBNet+CRNN: 正常 vs 低光照 vs SigLIP增强
        → outputs/phase2_comparison/
        → 结论: 增强恢复率 56%
```

---

### 三、Phase 2 核心结果

| 图片 | 正常 | 低光照 | SigLIP增强 | 变化 |
|------|------|--------|------------|------|
| densetext_det | 1724 | 1494 | 1651 | +11% |
| kie | 90 | 62 | 62 | 0% |
| text_det | 23 | 14 | 27 | +93% |
| text_ocr | 19 | 2 | 2 | 0% |
| text_recog | 19 | 0 | 0 | 0% |

**汇总**:

- 低光照导致平均检测数下降: 60.6 个/图
- SigLIP 语义增强恢复: 34.0 个/图
- **恢复率: 56%**

**分析**:

- 密集文本场景（densetext_det +93%, text_det +93%）效果显著,因为文字区域在热力图中占比较大,增强准确打在目标区域
- 稀疏/孤立文本场景（kie, text_ocr, text_recog）效果差,因为 20x20 粗粒度 grid 中文字像素占比过低,热力图响应被背景稀释

---

### 四、关键发现与论文可用的论点

1. **低光照确实导致 DBNet+CRNN 性能显著下降**: 平均检测数从 375 降至 314（降 16%），部分场景完全失效（text_recog: 19→0）
2. **SigLIP 语义特征对光照具有一定鲁棒性**: 低光照下"清晰文字" prompt 的响应减弱但未消失,仍能区分文字与背景区域
3. **SigLIP 引导的语义增强可以有效部分恢复性能**: 整体恢复率 56%,密集文本场景效果尤为突出
4. **当前方法的局限在于 patch 粒度过粗**: 20x20 grid 对小文本区域不敏感,后续可通过更细粒度的特征对齐改进

---

### 五、文件清单

| 文件 | 用途 |
|------|------|
| `create_lowlight_dataset.py` | 正常图 → 低光照退化数据集 |
| `p1.py` | Phase 1: 正常 vs 低光照 二元对比 |
| `siglip_prototype.py` | SigLIP 多 prompt 热力图定性分析 |
| `siglip_enhance.py` | SigLIP 语义引导增强生成 |
| `p2_compare.py` | Phase 2: 正常 vs 低光照 vs 增强 三元对比 |

---

### 六、后续计划

1. **扩充数据集**: 引入 IIIT5K（识别 ~3000 张）或 ICDAR2013（检测 ~462 张），将样本量从 5 张扩大到数百张
2. **深化方法**: 将 SigLIP 语义信号从推理增强升级为训练辅助 loss（特征知识蒸馏），让 backbone 端到端学习光照无关表达
3. **消融实验**: 对比纯亮度增强 vs SigLIP 语义增强,证明语义引导的增量价值
4. **细粒度热力图**: 尝试更高分辨率的 patch grid 或 pixel-level attention,改善稀疏文本场景
